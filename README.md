# ML Movie Recommendation Service

## Setup Instructions

Follow these steps to set up the project:

1. **Virtual Environment Setup**:
   ```bash
   python -m venv .venv

   source .venv/bin/activate

   pip install -r requirements.txt